# Exemplo de variáveis de ambiente
# NÃO coloque chaves secretas aqui no repositório real.
# Use o servidor para armazenar GEMINI_API_KEY e outras chaves privadas.

# URL pública do backend (cliente usa apenas para chamar APIs)
VITE_API_URL=https://api.seu-dominio.com

# Variáveis que começam com VITE_ serão embutidas no bundle e EXPÕEM-SE ao cliente.
# Não coloque segredos aqui.

# Em produção, mantenha chaves privadas em variáveis de ambiente do servidor:
# GEMINI_API_KEY=seu_token_privado_aqui
# JWT secret para tokens de autenticação (mantenha privado no servidor)
# JWT_SECRET=uma_chave_bem_grande_e_segura
# Origem do cliente (para CORS no servidor)
# CLIENT_ORIGIN=http://localhost:3000
# Database URL (Postgres). Exemplo local:
# DATABASE_URL=postgres://user:password@localhost:5432/mirelli_crm
